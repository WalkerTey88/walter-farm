/**
 * Gallery page for Walter Farm.
 * Displays photos and images from the farm. Currently contains placeholder
 * text; content will be populated from the database in later iterations.
 */
export default function GalleryPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-semibold">Gallery</h1>
      <p className="text-sm text-neutral-700">
        This page will display photos and images from Walter Farm.
        Data will be loaded from the database in a later phase.
      </p>
    </div>
  );
}